<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: (0..5), as: :radio_buttons, checked: 5, wrapper: :vertical_radio_and_checkboxes %>

  <%= f.submit "Send my review", class: "btn btn-primary"%>
<% end %>
